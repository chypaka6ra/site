# 🎊 Инструкция по кастомизации свадебного приглашения

## Быстрый старт

Чтобы изменить имена жениха и невесты, дату и расписание свадьбы, отредактируйте файл `wedding-replacer.js`:

### Шаг 1: Откройте `wedding-replacer.js`

### Шаг 2: Найдите строку с конфигурацией (примерно на строке 7-17)

```javascript
const config = {
  groomName: 'Василий',           // 👈 Измените имя жениха
  brideName: 'Мария',              // 👈 Измените имя невесты
  weddingDate: '10 июня 2026',     // 👈 Измените дату
  weddingDateShort: '10.06.2026',  // Сокращённый формат даты
  schedule: [
    { time: '13:00', event: 'Встреча гостей' },
    { time: '14:00', event: 'Торжественная церемония' },
    { time: '15:30', event: 'Фуршет и танцы' },
    { time: '18:00', event: 'Банкет' },
    { time: '23:00', event: 'Окончание' }
  ]
};
```

### Шаг 3: Сохраните файл и обновите страницу в браузере (Ctrl+F5)

## Примеры кастомизации

### Пример 1: Изменение только имён

```javascript
const config = {
  groomName: 'Иван',
  brideName: 'Елена',
  // остальное оставить как есть
};
```

### Пример 2: Изменение даты свадьбы

```javascript
const config = {
  weddingDate: '25 мая 2027',      // Новая дата
  weddingDateShort: '25.05.2027',  // Сокращённый формат
};
```

### Пример 3: Полная кастомизация

```javascript
const config = {
  groomName: 'Петр',
  brideName: 'Ольга',
  weddingDate: '1 января 2027',
  weddingDateShort: '01.01.2027',
  schedule: [
    { time: '12:00', event: 'Приём гостей' },
    { time: '13:00', event: 'Регистрация' },
    { time: '14:00', event: 'Торжественная церемония' },
    { time: '15:30', event: 'Фотосессия' },
    { time: '16:30', event: 'Банкет' },
    { time: '23:00', event: 'Конец праздника' }
  ]
};
```

## Форматы дат

- **Полная дата**: "25 мая 2027" (используется для основного отображения)
- **Сокращённая**: "25.05.2027" (используется в некоторых местах)

## Расписание дня

Расписание состоит из массива объектов с полями:
- `time` - время события (формат HH:MM)
- `event` - название события

Добавьте новое событие:
```javascript
{ time: '17:00', event: 'Танцевальное шоу' }
```

## Цвета и шрифты

Все цвета и шрифты определены в файле `styles.css`:
- **Основной цвет**: #820000 (тёмно-красный)
- **Фон**: #fbf7ee (кремовый)
- **Шрифт**: Montserrat Alternates

Если нужно изменить цвета, отредактируйте соответствующие значения в `styles.css`.

## Советы

1. **Сохраняйте изменения**: После редактирования обязательно сохраните файл
2. **Кэш браузера**: Если изменения не видны, очистите кэш (Ctrl+Shift+Delete)
3. **Проверьте форматирование**: Убедитесь, что не нарушена структура JavaScript
4. **Специальные символы**: Если используете символы вроде апострофа, экранируйте их: `\'`

## Требуемые файлы

Для работы кастомизации необходимо наличие следующих файлов:

- ✅ `weddingmaket5.html` - основной HTML файл
- ✅ `styles.css` - стили страницы
- ✅ `script.js` - основные скрипты
- ✅ `wedding-customization.css` - стили кастомизации
- ✅ `wedding-replacer.js` - скрипт замены текста
- ✅ `index.html` - точка входа (опционально)

## Тестирование

После изменения конфигурации:

1. Откройте браузер
2. Перейдите на страницу: `weddingmaket5.html`
3. Нажмите `Ctrl+F5` для полной очистки кэша
4. Проверьте, что все текст обновился правильно

## Откат изменений

Если что-то пошло не так, вы всегда можете:
1. Восстановить оригинальные значения из примера выше
2. Или загрузить файл из репозитория заново

## Помощь

Если возникли проблемы, проверьте:
- Правильность синтаксиса JavaScript (нет лишних запятых, скобок)
- Кодировку файла (должна быть UTF-8)
- Консоль браузера на ошибки (F12 → Console)

---

**Готово к использованию!** 🎉
